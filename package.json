{
  "name": "goldberg-achievements-tracker",
  "version": "1.0.3",
  "private": true,
  "scripts": {
    "dev": "millennium-ttc --build dev",
    "watch": "millennium-ttc --build dev --watch",
    "build": "millennium-ttc --build prod"
  },
  "dependencies": {
    "@steambrew/api": "latest",
    "@steambrew/client": "latest",
    "@steambrew/webkit": "latest"
  },
  "devDependencies": {
    "@steambrew/ttc": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest"
  }
}
